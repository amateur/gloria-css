.header {
  padding: 0 100px;
  position: absolute;
  top: 40px;
  left: 0;
  right: 0;
  z-index: 1;
}

/*
** Elements
*/

.header__logo {
  width: 100px;
  height: 100px;
  background-image: var(--logo);
  background-repeat: no-repeat;
  background-size: contain;
}
